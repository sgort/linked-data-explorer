# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test coverage reports (Jest + Vitest, both packages)
coverage

# Keep committed (environment configs)
# .env.development
# .env.acceptance
# .env.production

# TypeScript compiled output
*.js
*.js.map
*.d.ts
*.d.ts.map

# Hand-authored ambient declarations (not generated output) — keep tracked
!packages/backend/src/types/shacl-rdf.d.ts

# Hand-authored Jest config (not generated output) — keep tracked
!packages/backend/jest.config.js

# But keep the source JSON
!testData.json

# Ignore local overrides
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claudesync

# Claude Code local config & per-machine memory (machine-specific, not portable).
# Exception: .claude/commands/ is shared tooling (e.g. bump-release), tracked
# like ronl-business-api's convention — everything else here (settings,
# per-project memory) stays local.
.claude/*
!.claude/commands/
CLAUDE.md

# Local env overrides (committed envs are .env.development / .acceptance / .production)
.env

# flatten script & dir
flatten_directory.sh
flattened
