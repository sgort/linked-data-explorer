# Local Development Environment
# Used when running: npm run dev

VITE_API_BASE_URL=http://localhost:3001
VITE_OPERATON_BASE_URL=http://localhost:8081/engine-rest
# CPSV Editor (DSO → DMN publish handoff). Run the CPSV Editor on a non-3000
# port locally since LDE's dev server also uses 3000.
VITE_CPSV_EDITOR_URL=http://localhost:3002
